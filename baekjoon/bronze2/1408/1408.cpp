#include<iostream>
#include<string>

using namespace std;

int main() {
    string a, b;

    cin >> a >> b;

    int q, w, e, r, t, y;   

    q = stoi(a.substr(0, 2));
    w = stoi(a.substr(3, 2));
    e = stoi(a.substr(6, 2));
    r = stoi(b.substr(0, 2));
    t = stoi(b.substr(3, 2));
    y = stoi(b.substr(6, 2));

    
}