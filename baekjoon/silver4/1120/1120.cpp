#include<iostream>
#include<string>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    string a, b;
    cin >> a >> b;
    int min = 9999;
    for(int i = 0; i < a.length(); i++); {
        int tmp1, tmp2;
        
    }
    return 0;
}