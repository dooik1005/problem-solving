#include<iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    double arr[10000];
    cin >> n;
    for(int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    double max = 0;
    for(int i = 0; i < n; i++) {
        
    }

    
}